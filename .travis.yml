language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - pip install --user --upgrade pip
  - pip install --user -r requirements/travis.txt
  - pip install --user -e .
virtualenv:
  system_site_packages: true
script:
    - pytest tests --cov